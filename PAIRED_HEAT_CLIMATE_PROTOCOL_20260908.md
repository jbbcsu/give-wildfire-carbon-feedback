# Matched SSP585/SSP126 heat and rainfall inputs

Registered before acquisition and comparison. Source discovery returned one
public CC0 ISIMIP3b GFDL-ESM4/r1i1p1f1 W5E5 SSP585 daily Tmax dataset,
version20210512, DOI10.48364/ISIMIP.842396.1. New source config binds its actual
2041–2050 file identity and parent checksum; the source/request-only false
download flag is not a denial of the separate standing user authorization.
Do not mutate the SSP126 source, receipt, approval or climate bytes.

Submit one [-180,180,39,40] spatial cutout with no averaging. Retain the
request/partial receipt and never resubmit an uncertain request automatically.
When finished, verify its registered job URL, public catalogue, capped HEAD
size/ETag and unchanged GET identity, stream in256KiB chunks, inspect safe ZIP
members and reserve space before extraction. Reuse exact3652day/grid/unit/
finite-value checks. Per-batch additional disk <=64MiB, free space >=130GiB,
one numerical thread and <=1GiB sampled RAM. No full global downloads.

Construct maize29°C and soybean30°C seasonal/stage heat for2042–2049, both
resident calendars, one crop at a time. Exact cutout lineage must match the
rainfall scenario before joining. Reuse unchanged calendar/stage/season
validation and fixed MIRCA2000 allocation. No future outcomes. Account both
crop output directories plus the acquisition against64MiB.

Pair higher-minus-lower scenario by exact crop/grid/year, same model/member,
years, calendar weights and threshold. Report seasonal rainfall, weighted
log-rain index, longest dry spell, Rx5day and each stage heat count/integral;
average years within cell, then equally average cells. Also report per-cell
10th/90th percentiles and count; these are spatial dispersion, not uncertainty
intervals or geographic/production/value weights. Verify unchanged common
rainfall fields against the already completed climate basis.

These are same-model scenario contrasts with internal variability, not
isolated precipitation-attributable crop losses, a no-climate-change
counterfactual or an emissions-pulse derivative. Do not apply historical yield
slopes, silently clip heat, select models by these results, or calculate SCC.
