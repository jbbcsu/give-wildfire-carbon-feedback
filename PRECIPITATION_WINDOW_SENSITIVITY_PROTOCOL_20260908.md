# Exploratory window sensitivity, motivated by observed model disagreement

Registered before the additional calculations, but after seeing the opposing
GFDL/IPSL2042–2049 rainfall signs. This is not an ex-ante confirmatory test.
Use the existing fixed-weight future rainfall basis for maize/soybean and all
three balanced models(GFDL,IPSL,MPI). Compare SSP585 minus SSP126 separately
in2032–2039,2042–2049,2052–2059: eight-year windows at the start, middle and
end of the retained2032–2059period. Retain the previously completed28year
comparisons as references; do not reconstruct them or refit any model.

Use the existing exact-key/completeness and equal-cell comparison function.
Report seasonal rain, weighted log-rain index, longest dry spell and Rx5day.
All source data are already resident. No climate downloads, yield fitting,
model selection, standard errors or significance claims. Record direction
changes across windows and models without selecting the window that supports
a preferred claim. Differences also contain temporal evolution of forcing;
this cannot separate internal variability from forced change. MPI lacks the
new matched heat products, so do not fabricate them from stage mean T.

The2042–2049GFDL/IPSL summaries must match the completed joint-climate result
to1e-10absolute tolerance for shared rainfall variables. A mismatch stops
publication. Stream/read one small crop/model pair at a time, <=512MiB sampled
RAM, numeric threads1, tiny aggregate output only.
